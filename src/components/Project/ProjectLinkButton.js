import React from "react"

const ProjectLinkButton = ({ link }) => (
  <button href={link} class={'w-32 github-button rounded-lg h-10 flex items-center justify-center mr-4'}>
    button
  </button>
)

export default ProjectLinkButton